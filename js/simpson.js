define(function() {
    /**
     * Integrates expression from b to a with N steps using Simpson's rule.
     *
     * @param {string} function to integrate. Must be a valid Math.js expression.
     * @param {number} Upper domain value.
     * @param {number} Lower domain value.
     * @param {number} Integration steps.
     *
     * @return {number} Integral value.
     */
   return {
       integrate: function(expression, b,a, N) {
            return 0.5
       }
   }
});